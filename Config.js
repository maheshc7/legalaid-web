
const config = {
    appId: process.env.APP_ID,
    redirectUri: process.env.APP_REDIRECT_URL,
    scopes: process.env.APP_SCOPES,
    backend_url: process.env.BACKEND_URL
  };
  
  export default config;
  